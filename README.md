Run with `cargo run`

```
thread 'main' panicked at src/main.rs:25:7:
called `Result::unwrap()` on an `Err` value: ExifParseError(TagNotFound(50714))
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
```
